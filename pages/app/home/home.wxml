<import src="../../../components/recommondlist.wxml"></import>
<view class='container'>
  <view class='index_searchBar' bindtap='goSearch'>
    <image class='index_searchBtn'></image>
    <text class='index_searchText'>搜索</text>
  </view>
  <view class='navBar'>
    <view>最热</view>
    <view>人物</view>
    <view>实验</view>
    <view>治愈系</view>
    <view>事件</view>
  </view>
  <template is="recommondlist" data="{{item,video}}" wx:for="{{movieList}}" wx:key="{{index}}"></template>
  <loadmore wx:if="{{isLoading}}" />
</view>