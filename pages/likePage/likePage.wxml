<!--pages/likePage/likePage.wxml-->
<view wx:if="{{!videoList.length}}" class="noData">空空如也~</view>
<view wx:for="{{videoList}}" class='list' data-item="video">
  <view class='title'>{{item.title}}</view>
  <view class='bottom'>
    <view>{{item.jointime}}</view>
  </view>
</view>
